# AGENTS.md

## 用户长期要求

之后用户提出的每次内容生成类需求，都应默认生成对应的 `.md` 文件。

执行时遵循：

- 根据用户要求判断应生成的 Markdown 文件名。
- 直接在当前工作目录创建或更新对应的 `.md` 文件。
- 文件内容使用清晰的 Markdown 结构。
- 如果用户明确指定文件名，以用户指定为准。
- 如果用户没有指定文件名，根据需求主题生成简洁、准确的中文文件名。
- 完成后告知用户生成的文件路径。

## 课程内容目录

详细课程内容应默认放入当前工作目录下的 `课程内容/` 文件夹。

例如：

- `课程内容/第1课-项目全貌与学习路线.md`
- `课程内容/第2课-开发环境准备.md`

## 教程项目代码修改规则

本项目是学习教程项目。

当用户讨论课程、教程、学习步骤、示例代码或指出课程与代码不一致时，默认不要直接替用户修改项目代码文件。

执行时遵循：

- 优先修改课程 Markdown、补充讲解、整理练习步骤或指出应如何改。
- 不要把练习答案直接写进 `page.tsx` 等源码文件，让用户失去练习过程。
- 如果确实需要修改源码，必须先确认用户明确要求“直接改代码”“帮我实现”“修代码”等。
- 如果用户只是指出问题、要求检查、要求解释或要求整理教程，默认只处理文档和说明。

## 教程内容衔接规则

编写或修改课程教程时，必须保证课与课之间的内容、代码和操作步骤能自然衔接。

执行时遵循：

- 新课程默认基于上一课结束时的真实代码状态继续推进。
- 示例代码、文件路径、组件名、状态变量和页面结构要与当前项目代码对应。
- 如果课程要替换、迁移或拆分上一课已有代码，应直接写清楚具体操作。
- 不要在教程正文中写“先注意一个衔接点”“第几课结束后”“第几课目标”“更合理的调整顺序是”这类元说明。
- 教程读者应默认感受到内容和代码是连续的，正文直接说明当前文件是什么、接下来怎么做、为什么这样处理。
- 例如：可以写“`app/page.tsx` 目前还是账户管理页，不是首页。不要丢掉这段代码，而是把它迁移到 `app/dashboard/page.tsx` 承接账户管理功能。”
