# AGENTS.md

## 用户长期要求

之后用户提出的每次内容生成类需求，都应默认生成对应的 `.md` 文件。

执行时遵循：

- 根据用户要求判断应生成的 Markdown 文件名。
- 直接在当前工作目录创建或更新对应的 `.md` 文件。
- 文件内容使用清晰的 Markdown 结构。
- 如果用户明确指定文件名，以用户指定为准。
- 如果用户没有指定文件名，根据需求主题生成简洁、准确的中文文件名。
- 完成后告知用户生成的文件路径。

## 课程内容目录

详细课程内容应默认放入当前工作目录下的 `课程内容/` 文件夹。

例如：

- `课程内容/第1课-项目全貌与学习路线.md`
- `课程内容/第2课-开发环境准备.md`

## 整理笔记任务

当用户说“整理笔记”，并附上标题和原始内容时，默认理解为：

- 根据用户提供的标题创建或更新对应的 Markdown 文件。
- 文件默认放入 `课程内容/` 文件夹。
- 文件名使用用户给出的标题，格式为 `标题.md`。
- 如果用户明确指定其他路径或文件名，以用户指定为准。
- 对原始内容进行适当精简、归类和优化，不逐字照搬。
- 保留关键概念、核心结论、必要示例和易错点。
- 使用清晰的 Markdown 结构，包括标题、分节、列表、代码块、表格或引用说明。
- 代码示例应尽量简洁、可读，并保持语法正确。
- 输出风格保持简洁、适合复习，避免过度口语化和冗长解释。
- 完成后告知用户生成或更新的文件路径。
