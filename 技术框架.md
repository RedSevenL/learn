# AI 个人财务 CFO 技术框架

## 1. 项目目标

本项目定位为本地优先的 **AI 个人财务 CFO**，核心目标不是简单记账，而是回答用户在财务决策中的两个问题：

- **该怎么花**：基于收入、支出、资产、负债、目标和风险偏好，给出可执行的现金流建议。
- **选择会带来什么后果**：通过确定性计算引擎量化不同消费、储蓄、还债、投资假设对未来财务状态的影响。

系统设计遵循一个核心原则：

> LLM 只负责语言理解、参数抽取和回复组织；所有金额、利率、现金流、目标达成率、偿债路径等关键结果必须由确定性 TypeScript 计算引擎产生，并可追溯。

## 2. 技术栈选型

| 层级 | 技术 | 作用 |
| --- | --- | --- |
| 应用框架 | Next.js 15 | 使用 App Router 构建全栈应用，承载页面、Server Actions、Route Handlers |
| 语言 | TypeScript | 统一前后端类型，降低财务计算和数据结构漂移风险 |
| UI | React + TailwindCSS | 构建对话界面、财务仪表盘、场景模拟面板 |
| 组件 | shadcn/ui 或 Radix UI | 表单、弹窗、Tabs、表格、Popover 等基础交互组件 |
| 图表 | Recharts 或 ECharts | 现金流曲线、资产负债趋势、目标进度、场景对比 |
| ORM | Drizzle ORM | 类型安全访问 SQLite，迁移可控 |
| 数据库 | SQLite | 本地优先、零部署、适合个人财务数据 |
| 精度计算 | decimal.js | 避免浮点误差，所有金额和利率计算使用 Decimal |
| 校验 | Zod | API 入参、LLM JSON 输出、CSV 导入数据校验 |
| LLM | DeepSeek API | 意图识别、参数抽取、自然语言解释 |
| CSV | Papa Parse 或 csv-parse | 导入银行、支付宝、微信账单 |
| 测试 | Vitest + Testing Library | 覆盖计算引擎、API、核心 UI 行为 |

## 3. 总体架构

系统采用单体全栈架构，优先保证开发效率、数据私密性和可审计性。

```txt
Next.js App
├── 前端交互层
│   ├── 对话界面
│   ├── 财务面板
│   ├── 场景模拟器
│   └── 数据导入与修正界面
│
├── 应用服务层
│   ├── Chat Service
│   ├── Intent Service
│   ├── Finance Data Service
│   ├── Simulation Service
│   └── Audit Service
│
├── 领域计算层
│   ├── 现金流预测
│   ├── 复利与目标规划
│   ├── 债务偿还策略
│   ├── 预算与储蓄率分析
│   └── What-if 场景对比
│
├── 数据层
│   ├── SQLite
│   ├── Drizzle schema
│   ├── 数据迁移
│   └── CSV 导入映射
│
└── 外部服务层
    ├── DeepSeek API
    └── 可选 Go 数据管道 API
```

## 4. 推荐目录结构

```txt
app/
├── page.tsx
├── dashboard/
│   └── page.tsx
├── scenarios/
│   └── page.tsx
├── import/
│   └── page.tsx
├── api/
│   ├── chat/route.ts
│   ├── intent/route.ts
│   ├── transactions/route.ts
│   ├── accounts/route.ts
│   ├── liabilities/route.ts
│   ├── simulations/route.ts
│   └── imports/csv/route.ts
│
components/
├── chat/
├── dashboard/
├── finance/
├── import/
└── ui/
│
lib/
├── db/
│   ├── client.ts
│   ├── schema.ts
│   └── migrations/
├── llm/
│   ├── deepseek.ts
│   ├── prompts.ts
│   └── intent-schema.ts
├── services/
│   ├── chat-service.ts
│   ├── finance-service.ts
│   ├── simulation-service.ts
│   └── audit-service.ts
├── engine/
│   ├── money.ts
│   ├── cashflow.ts
│   ├── compound.ts
│   ├── debt.ts
│   ├── goals.ts
│   ├── scenarios.ts
│   └── explanations.ts
├── importers/
│   ├── csv-parser.ts
│   ├── bank-mapper.ts
│   └── category-rules.ts
└── validators/
    ├── finance.ts
    ├── api.ts
    └── csv.ts
```

## 5. 核心模块设计

### 5.1 对话与意图解析

对话系统分为三步：

1. 用户输入自然语言问题。
2. LLM 将问题转换为结构化意图。
3. 后端根据意图调用确定性计算引擎，再将计算结果交给 LLM 组织自然语言回复。

示例意图结构：

```ts
type FinanceIntent =
  | {
      type: "savings_goal";
      targetAmount: string;
      deadlineMonths: number;
      initialAmount?: string;
    }
  | {
      type: "debt_payoff";
      debtIds?: string[];
      strategy: "snowball" | "avalanche" | "custom";
      monthlyBudget?: string;
    }
  | {
      type: "cashflow_forecast";
      months: number;
      scenarioId?: string;
    }
  | {
      type: "what_if";
      changes: ScenarioChange[];
      compareTo: "current_baseline";
    };
```

LLM 输出必须经过 Zod 校验。校验失败时不进入计算引擎，而是要求模型重试或向用户追问缺失参数。

### 5.2 确定性计算引擎

计算引擎是项目核心，要求纯函数化、无副作用、可测试。

主要能力：

- **金额计算**：统一 Money 类型，内部使用 decimal.js。
- **现金流预测**：按月汇总收入、固定支出、可变支出、债务还款、储蓄和投资。
- **目标规划**：计算目标金额、期限、当前余额、月储蓄额、收益率之间的关系。
- **复利计算**：支持月度、年度利率换算，支持定投和一次性本金。
- **债务策略**：支持雪球法、雪崩法、自定义优先级。
- **场景对比**：将用户的消费、收入、利率、储蓄率变化转化为结果差异。

计算函数示例：

```ts
type GoalInput = {
  currentAmount: string;
  targetAmount: string;
  monthlyContribution: string;
  annualReturnRate: string;
  months: number;
};

type GoalResult = {
  projectedAmount: string;
  targetMet: boolean;
  shortfall: string;
  requiredMonthlyContribution: string;
  steps: CalculationStep[];
};
```

每个计算结果都应返回 `steps`，用于解释公式、参数和中间结果。

### 5.3 财务数据管理

数据管理模块负责账户、资产、负债、流水、分类和预算。

核心实体：

- **Account**：现金、银行卡、信用卡、投资账户、贷款账户。
- **Transaction**：收入、支出、转账、还款、投资买入卖出。
- **Liability**：贷款、信用卡分期、消费贷、房贷、车贷。
- **Budget**：按月、按分类设置预算。
- **Goal**：储蓄目标、还债目标、大额消费目标。
- **Scenario**：场景参数快照。
- **CalculationHistory**：计算审计记录。

数据写入原则：

- 金额字段以字符串存储，避免 JS number 精度问题。
- 所有流水保留原始导入文本，便于回溯。
- 自动分类必须允许用户手动修正。
- 删除关键数据优先软删除，避免审计链断裂。

## 6. 数据库模型草案

```ts
export const accounts = sqliteTable("accounts", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  type: text("type").notNull(),
  currency: text("currency").notNull().default("CNY"),
  balance: text("balance").notNull(),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
  updatedAt: integer("updated_at", { mode: "timestamp" }).notNull(),
});

export const transactions = sqliteTable("transactions", {
  id: text("id").primaryKey(),
  accountId: text("account_id").references(() => accounts.id),
  occurredAt: integer("occurred_at", { mode: "timestamp" }).notNull(),
  amount: text("amount").notNull(),
  direction: text("direction").notNull(), // income | expense | transfer
  categoryId: text("category_id"),
  merchant: text("merchant"),
  note: text("note"),
  source: text("source").notNull(), // manual | csv | api
  rawPayload: text("raw_payload"),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
});

export const liabilities = sqliteTable("liabilities", {
  id: text("id").primaryKey(),
  name: text("name").notNull(),
  principal: text("principal").notNull(),
  remainingPrincipal: text("remaining_principal").notNull(),
  annualRate: text("annual_rate").notNull(),
  minimumPayment: text("minimum_payment"),
  dueDay: integer("due_day"),
  startDate: integer("start_date", { mode: "timestamp" }),
  endDate: integer("end_date", { mode: "timestamp" }),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
});

export const calculationHistory = sqliteTable("calculation_history", {
  id: text("id").primaryKey(),
  intentType: text("intent_type").notNull(),
  inputJson: text("input_json").notNull(),
  formulaJson: text("formula_json").notNull(),
  outputJson: text("output_json").notNull(),
  modelTraceJson: text("model_trace_json"),
  createdAt: integer("created_at", { mode: "timestamp" }).notNull(),
});
```

## 7. API 设计

### 7.1 Chat API

`POST /api/chat`

职责：

- 接收用户消息。
- 调用 DeepSeek 做意图解析。
- 读取必要财务数据。
- 调用计算引擎。
- 保存计算历史。
- 返回自然语言回复、结构化结果和可视化数据。

返回示例：

```json
{
  "message": "按当前储蓄速度，两年后预计可达到 523000 元，超过目标 23000 元。",
  "intent": {
    "type": "savings_goal",
    "targetAmount": "500000",
    "deadlineMonths": 24
  },
  "result": {
    "projectedAmount": "523000",
    "targetMet": true,
    "shortfall": "0"
  },
  "auditId": "calc_01h..."
}
```

### 7.2 财务数据 API

- `GET /api/accounts`
- `POST /api/accounts`
- `GET /api/transactions`
- `POST /api/transactions`
- `POST /api/imports/csv`
- `GET /api/liabilities`
- `POST /api/liabilities`
- `POST /api/simulations`
- `GET /api/calculations/:id`

所有写接口必须使用 Zod 校验，并在服务层完成 Decimal 格式归一化。

## 8. LLM 调用边界

LLM 允许做：

- 自然语言意图分类。
- 金额、日期、目标、约束条件抽取。
- 对计算结果进行解释。
- 当参数不足时生成追问。

LLM 禁止做：

- 直接输出最终金额结论。
- 自行计算复利、偿债周期、目标达成率。
- 修改用户原始财务数据。
- 在未保存审计记录时生成不可追踪建议。

建议采用两段式调用：

1. **Intent Call**：要求模型只输出 JSON。
2. **Narration Call**：输入确定性结果，让模型组织用户可读回复。

## 9. 审计与可解释性

每次计算都生成审计记录，至少包含：

- 用户原始问题。
- LLM 解析出的结构化意图。
- 参与计算的数据快照 ID 或数据摘要。
- 计算函数名称和版本。
- 输入参数。
- 公式步骤。
- 中间结果。
- 最终结果。
- 回复文本。

前端应提供“查看计算过程”入口，展示：

- 用了哪些账户、流水、负债。
- 用了哪些假设。
- 每一步公式。
- 哪些参数来自用户输入，哪些参数来自历史数据。

## 10. CSV 导入流程

```txt
上传 CSV
  ↓
识别来源格式：银行 / 支付宝 / 微信 / 通用模板
  ↓
字段映射：日期、金额、方向、商户、备注
  ↓
数据校验：日期合法、金额合法、重复检测
  ↓
自动分类：规则匹配 + 可选 LLM 辅助分类
  ↓
用户确认
  ↓
写入 transactions
```

重复检测建议使用：

- 日期。
- 金额。
- 商户。
- 原始备注。
- 来源文件 hash。

## 11. 前端页面设计

### 11.1 对话页

核心区域：

- 左侧或主区域为对话流。
- 右侧展示当前问题的计算卡片、假设参数和图表。
- 每条 AI 回复附带 `查看计算过程` 和 `创建场景` 操作。

### 11.2 财务面板

指标：

- 净资产。
- 月收入。
- 月支出。
- 储蓄率。
- 负债率。
- 现金流安全月数。
- 目标完成进度。

图表：

- 资产负债趋势。
- 月度现金流。
- 分类支出占比。
- 未来 12/24/36 个月预测。

### 11.3 场景模拟页

支持用户调整：

- 月收入变化。
- 固定支出变化。
- 大额消费。
- 还债预算。
- 投资收益率。
- 储蓄目标。

输出：

- 基准场景 vs 模拟场景。
- 目标完成时间变化。
- 净资产差异。
- 现金流风险月份。

## 12. 测试策略

优先覆盖计算引擎，因为它是系统可信度核心。

测试层级：

- **单元测试**：复利、现金流、债务策略、目标规划。
- **契约测试**：LLM 意图 JSON schema、API 入参 schema。
- **集成测试**：CSV 导入到交易入库、Chat API 到计算历史保存。
- **快照测试**：典型财务问题的计算步骤和输出结构。

关键测试用例：

- 0 收入、0 支出、负现金流。
- 信用卡高利率债务优先偿还。
- 目标期限小于当前能力导致缺口。
- CSV 重复导入。
- LLM 输出缺字段或非法金额。

## 13. 安全与隐私

由于财务数据高度敏感，默认采用本地优先策略。

措施：

- SQLite 数据库默认保存在本机。
- DeepSeek 调用只发送完成任务所需的最小上下文。
- 不将完整流水明细发送给 LLM，优先发送聚合结果。
- API key 使用环境变量，不写入仓库。
- 计算历史中保存模型输入摘要，避免泄露过量原始隐私数据。
- 可选增加数据库文件加密或应用级导出密码。

`.env.local` 示例：

```txt
DEEPSEEK_API_KEY=...
DATABASE_URL=file:./local.db
```

## 14. 部署形态

推荐阶段性部署：

1. **本地开发版**：Next.js + SQLite，适合个人使用和验证产品闭环。
2. **桌面封装版**：可考虑 Tauri，将本地数据库和 Web UI 打包为桌面应用。
3. **私有云版**：迁移到 Postgres，支持多用户和设备同步。
4. **混合版**：核心财务数据本地保存，云端只保存加密备份或模型配置。

## 15. 开发里程碑

### M1：可信计算 MVP

- 建立 Next.js 项目。
- 建立 SQLite + Drizzle schema。
- 实现账户、流水、负债基础 CRUD。
- 实现储蓄目标、现金流预测、债务偿还三类计算。
- 为计算引擎补齐单元测试。

### M2：对话闭环

- 接入 DeepSeek API。
- 实现意图解析 JSON schema。
- 实现 `POST /api/chat`。
- 支持“我多久能攒够 X 元”、“这笔消费会影响什么”、“怎么还债更快”等问题。
- 保存并展示计算历史。

### M3：数据导入与面板

- 支持 CSV 导入。
- 实现重复检测与分类修正。
- 建立财务仪表盘。
- 增加现金流、支出分类、净资产趋势图。

### M4：场景模拟

- 实现场景参数编辑。
- 支持基准场景与模拟场景对比。
- 支持保存模拟记录。
- 在对话中一键生成场景。

### M5：隐私与产品化

- 增加本地数据导出和备份。
- 增加 API key 配置界面。
- 增加数据库加密方案评估。
- 评估 Tauri 桌面封装。

## 16. 首批可实现的问题模板

建议优先支持以下问题，因为它们能直接验证产品价值：

- 我两年内能攒够 50 万吗？
- 如果我每月多花 3000 元，一年后会少攒多少钱？
- 我应该先还哪笔债？
- 我现在的现金流安全吗？
- 这笔 2 万元消费会不会影响我年底目标？
- 如果收入下降 20%，我能撑几个月？
- 我每月要存多少钱才能在 18 个月内完成目标？

## 17. 关键风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| LLM 幻觉 | 给出错误财务建议 | 限制 LLM 只解析和表达，结果必须来自计算引擎 |
| 金额精度错误 | 破坏可信度 | 全链路使用 Decimal 和字符串金额 |
| CSV 格式复杂 | 导入失败率高 | 先支持通用模板，再逐步支持具体平台 |
| 用户数据敏感 | 使用门槛上升 | 本地优先、最小化发送给 LLM、提供透明说明 |
| 计算不可解释 | 用户不信任建议 | 保存公式步骤和输入输出审计记录 |

## 18. 推荐优先级

第一阶段不要急于做复杂投资分析或市场数据接入。应优先完成：

1. 本地数据模型。
2. 可信计算引擎。
3. 意图解析到计算的闭环。
4. 计算过程可视化。
5. CSV 导入和基础面板。

当这些能力稳定后，再接入 Go 管道 API 获取市场数据、K 线、新闻或更复杂的外部资产价格。
