# 交接文档

工作空间：`/Users/zisu/dev/learn`

## 用户长期规则

- 全程中文工作和输出。
- 用户说“记忆”“规则”时，指项目里的 `AGENTS.md`，不要使用本地记忆。
- 内容生成类需求默认创建或更新 `.md` 文件。
- 课程内容默认放在 `课程内容/`。
- 整理笔记默认放在 `课程内容/基础笔记/` 或其子目录。
- 本项目是学习教程项目。用户讨论课程、教程、示例代码、代码与课程不一致时，默认不要直接替用户改源码；优先改文档、补讲解、整理步骤。只有用户明确说“直接改代码”“帮我实现”“修代码”时才动源码。
- 写教程时必须保证课与课、代码与文档自然衔接。不要写“先注意一个衔接点”“第几课目标”这类元说明，直接写当前文件是什么、接下来怎么做、为什么。

## 当前项目状态

- 主项目：`ai-finance-cfo/`
- 当前课程进度：第 13 课已创建，前面课程已整理。
- 最近一次提交主题：整理基础笔记结构。
- 本轮工作要求：把当前未提交的学习脉络笔记修改合并进上一次提交，并写本交接文档。

## 当前目录整理情况

基础笔记已按主题整理：

```txt
课程内容/基础笔记/
  编写思路/
  语法知识/
```

重要文件：

- `课程内容/基础笔记/编写思路/第7课到第11课学习脉络.md`
- `课程内容/基础笔记/编写思路/CFO项目结构说明.md`
- `课程内容/基础笔记/编写思路/HTML-找大数游戏指引.md`
- `课程内容/基础笔记/编写思路/TS-代码练习.md`
- `课程内容/第13课-HTTP与API入门.md`

## 刚完成的重点整理

`第7课到第11课学习脉络.md` 已被整理成阶段复盘主笔记。

当前结构按三个核心角度组织：

1. 代码结构
2. 思考及编写顺序
3. 功能流程

并补入了用户自己的“编程小记”中适合保留的内容：

- JS 基础：变量、模板字符串、布尔判断、数组、对象、`if`、`for...of`、函数。
- TS 基础：类型标注、函数参数与返回值、对象类型、可选字段、联合类型、对象数组。
- React 对应：props、`.map()`、`useState`、展开语法、受控组件和 `onChange`。
- 工具链：Node.js、npm、npx、`npm run dev` 背后的流程。

也修正了几个容易误解的点：

- 分号不是“语句块必须有”，而是语句结尾常见写法。
- `interface` 不写 `=`，`type` 才写 `=`。
- `onChange` 不是自己决定输入框最终显示；最终显示由 `value={name}` 和 React 状态共同决定。

## 当前学习脉络主线

用户目前在把前面几段学习串起来：

```txt
TS / DOM 练习
  ↓
找大数游戏
  ↓
React 组件
  ↓
React 状态与表单
  ↓
TailwindCSS 基础 UI
  ↓
Next.js 多页面路由
  ↓
项目页面骨架
  ↓
HTTP 与 API
```

用户关注点：

- 不只是记语法，而是理解从需求到代码的结构。
- 容易混淆 TS/DOM 练习、找大数游戏、React 项目之间的概念交叉。
- 喜欢按“代码结构、编写顺序、功能流程”来整理。

## 代码项目状态

`ai-finance-cfo/` 当前已经有多页面骨架：

```txt
app/
  layout.tsx
  page.tsx
  chat/page.tsx
  dashboard/page.tsx
  import/page.tsx
  scenarios/page.tsx
lib/
  mock-data.ts
```

用户前面已经完成：

- 首页入口
- 公共导航
- `/chat`
- `/dashboard`
- `/import`
- `/scenarios`
- `lib/mock-data.ts`

涉及源码时，除非用户明确要求，不要直接改代码。

## 建议下一个对话接手方式

如果用户继续学习第 13 课：

1. 先看 `课程内容/第13课-HTTP与API入门.md`。
2. 如果用户指出课程与代码不衔接，优先修课程文档。
3. 如果用户要求解释 API、HTTP、fetch，要联系前面的 TS 代码练习中 `fetch`、`async/await`、`getJSON<T>()`。
4. 如果用户要写代码，先确认是否明确要求改源码；教程练习不要直接替他写完。

如果用户要求提交：

- 先 `git status --short`。
- 涉及 `ai-finance-cfo` 源码时跑 `npm run lint`。
- 暂存/提交可能需要 escalated 权限写 `.git/index.lock`。
